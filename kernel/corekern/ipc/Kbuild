# SPDX-License-Identifier: GPL-2.0-only

obj-y += airy_ck_ipc.o

airy_ck_ipc-y := airy_ipc_ring.o airy_ipc_fastpath.o airy_ipc_zero_copy.o

# P1-8 KUnit regression for cancelBadgedSends.  The ring entry points are
# non-static (airy_ipc_internal.h), so airy_ipc_ring.o stays in the link.
airy_ck_ipc-$(CONFIG_AIRY_KUNIT) += airy_ipc_ring_test.o
